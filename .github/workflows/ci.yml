name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'

    - name: Install X11 dependencies (for ebiten/gui build)
      run: |
        sudo apt-get update -q
        sudo apt-get install -y libasound2-dev libgl1-mesa-dev libx11-dev libxcursor-dev libxi-dev libxinerama-dev libxrandr-dev libxxf86vm-dev

    - name: Run tests
      run: go test -v -race -coverprofile=coverage.txt -covermode=atomic ./...

    - name: Upload coverage
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.txt
        fail_ci_if_error: false

  integration-test:
    name: Integration Test
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'

    - name: Install X11 dependencies (for ebiten/gui build)
      run: |
        sudo apt-get update -q
        sudo apt-get install -y libasound2-dev libgl1-mesa-dev libx11-dev libxcursor-dev libxi-dev libxinerama-dev libxrandr-dev libxxf86vm-dev

    - name: Build tools
      run: |
        go build -o bin/chaser-server ./cmd/chaser-server
        go build -o bin/chaser-mapgen ./cmd/chaser-mapgen
        go build -o bin/chaser-server-gui ./cmd/chaser-server-gui

    - name: Generate test map
      run: |
        ./bin/chaser-mapgen -b 5 -i 5 -o ./testdata 1

    - name: Run integration test
      run: |
        chmod +x ./scripts/integration-test.sh
        ./scripts/integration-test.sh

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'

    - name: Install X11 dependencies (for ebiten/gui build)
      run: |
        sudo apt-get update -q
        sudo apt-get install -y libasound2-dev libgl1-mesa-dev libx11-dev libxcursor-dev libxi-dev libxinerama-dev libxrandr-dev libxxf86vm-dev

    - name: Run golangci-lint
      uses: golangci/golangci-lint-action@v4
      with:
        version: latest

  build:
    name: Build
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.24'

    - name: Install X11 dependencies (Linux only, for ebiten/gui build)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update -q
        sudo apt-get install -y libasound2-dev libgl1-mesa-dev libx11-dev libxcursor-dev libxi-dev libxinerama-dev libxrandr-dev libxxf86vm-dev

    - name: Build client library
      run: go build ./chaser

    - name: Build server
      run: go build ./cmd/chaser-server

    - name: Build mapgen
      run: go build ./cmd/chaser-mapgen

    - name: Build GUI server
      run: go build ./cmd/chaser-server-gui

    - name: Build examples
      run: |
        cd examples/test1 && go build .
        cd ../test2 && go build .
        cd ../test3 && go build .
